@using PolymorphicModelBinder.Html
@model DynamicPolymorphicListViewModel

@for (var i = 0; i < Model?.Pets.Count; i++)
{
    @Html.PolymorphicTypeInValueFor(x => x.Pets[i])
    @Html.PolymorphicEditorFor(x => x.Pets[i])
}